class positionMonitor {
    constructor(x,y,textSize,fill,stroke,weight) {
        this.x = x;
        this.y = y;
        this.textSize = textSize;
        this.fill = fill;
        this.stroke = stroke;
        this.weight = weight;

        //add constant which changes with textsize for text positions
        text();
    }

    
}